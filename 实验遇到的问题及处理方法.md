# 实验遇到的问题及处理方法

本文件记录实验过程中实际遇到并解决的问题，以及每个问题背后的原因和最终采用的
处理方式，供答辩与复习使用。章节号引用的是 `射影矩阵自编程实现与透视矫正实验报告.md`。

---

## 一、方程与求解

### 问题 1：点数不足或三点共线，方程无解且原先不报错

**现象与原因。** 每对点提供两个独立方程，`H` 有 8 个自由度，所以至少需要 4 对点，
且任意 3 点不能共线。当角点选得退化时，DLT 矩阵的秩低于 8，零空间不再是 1 维，
`vh[-1]` 只是零空间里任意一个方向，解出来的 `H` 没有意义。
危险之处在于流程不会自己停下来：矩阵形状始终是 `2n×9`，SVD 总能返回一个"解"。

**处理方法。** 在求解前做两次判据，并把原因分开报出（见问题 12）。

### 问题 2：齐次方程 `A h = 0` 的解从哪里取

**现象与原因。** 方程是齐次的，约束 `‖h‖ = 1` 后要最小化 `‖A h‖`。
这个最小化问题的解是 `AᵀA` 最小特征值对应的特征向量。

**处理方法。** 对 `A` 做 SVD `A = UΣVᵀ`，取 `V` 的最后一行（即 `vh[-1]`）。
因为 `AᵀA = VΣ²Vᵀ`，`V` 的最后一列就是 `AᵀA` 最小特征值对应的特征向量，
两者是同一个解。

### 问题 3：`h33 = 1` 这个约定什么时候失效

**现象与原因。** 令 `h33 = 1` 是常见的写法，它利用齐次坐标的尺度不确定性把待解向量
从 9 个分量压到 8 个。但真实射影矩阵可能出现 `h33 = 0`（此时源点原点被映到无穷远），
这时不存在 `h33 = 1` 的解，该约定结构性失效。

**处理方法。** 主实现采用 9 未知量的齐次 SVD，不固定 `h33`，因此对该情形免疫；
同时保留 `h33 = 1` 的非齐次解作为理论对照。

---

## 二、图像重采样

### 问题 4：前向映射会在目标图留下空洞

**现象与原因。** 把每个源像素按 `H` 搬到目标位置时，只要存在缩放或斜视，
多个源像素会落到同一个目标像素上，而另一些目标像素收不到任何源像素，
输出图上出现规则的空洞。

**处理方法。** 改用逆向映射：对每个目标整数像素反算源坐标，再双线性采样。
这样每个目标像素都被恰好处理一次，输出完整。源坐标落在图像外或映射到无穷远的点
填充背景色。

---

## 三、评价指标

### 问题 5：四点场景下"重投影误差"恒为 0，判断不了解的质量

**现象与原因。** 4 对点给出 8 个独立方程，而 `H` 恰好有 8 个自由度，方程数等于未知数，
是恰定方程组。解唯一且精确穿过每个对应点，所以拟合残差在数学上恒等于 0。
实测三个方法分别为 7.47e−09 / 3.16e−13 / 5.95e−14，相差 5 个数量级，
但全都是机器零。如果按这一列给方法排序，会得出"基础 DLT 比 OpenCV 差十万倍"这种
明显错误的结论。

**处理方法。** 增加留出点转移误差作为主要指标：在源四边形内部取一组不参与拟合的
双线性网格点，分别经估计矩阵与真值矩阵投影，量两者距离（见报告 §7.4）。
同时把拟合残差改名为 `fit_residual_*` 并标注它在 n=4 时没有鉴别力，
只在 n>4 的超定情形下才作为有效指标。

### 问题 6：手写结果与 OpenCV 几乎相同，容易被误读成"方法没有区别"

**现象与原因。** 两者的图像 MAE 只有约 1.2e−04 灰度级，视觉上完全一致。
这个数字看起来"没有差别"，容易被当成异常或当成"方法对比没做出来"。

**处理方法。** 把它定位为实现一致性的交叉验证：四点恰定时 `H` 唯一，
两者的几何映射本来就该是同一个矩阵，插值方式也同为双线性。
所以"几乎相同"恰恰是手写实现正确的证据。要观察方法差异，
必须进入 n>4 或大坐标量级的场景。

### 问题 7：归一化的作用被误认为"提高精度"

**现象与原因。** 归一化 DLT 在常规像素坐标下与基础 DLT 的结果几乎不可分辨，
四点时甚至解出完全相同的 `H`。如果按"误差是否变小"来判断，会得出
"归一化没用"的结论。

**处理方法。** 改为从条件数角度陈述它的作用：实测把 DLT 矩阵的条件数
从 1.13e6 降到 3.88。它的价值在坐标量级极端时体现——坐标放大到 1e4 倍后，
基础 DLT 的成功率降到 42%，1e5 倍时完全失败，而归一化版本始终 100% 成功。
因此结论写成"归一化改善的是数值稳定性，不是精度，也替代不了准确的角点标定"。

---

## 四、误差来源与优先级

### 问题 8：点数增加、噪声降低之后，仍存在约 95 像素的误差

**现象与原因。** 这 95 像素与点数和噪声都无关，它来自目标矩形（输出宽高比）的选择。
四点对应只能把源四边形映到"你所指定的那个矩形"上，`H` 的估计是确定的，
但正确宽高比必须先验给定。合成样本的真实文档尺寸是 640×420，
按对边平均长度自动估计得到 751×535，宽高比偏差 7.88%，真实几何误差 95.51 px。

**处理方法。** 三条途径，本实验演示了第一条：已知目标真实尺寸时用
`--output-size` 直接固定；已知宽高比时按比例反推；或者用消失线做度量矫正
（属于射影变换之外的拓展，本实验未实现）。

### 问题 9：角点精度与目标矩形的优先级

**现象与原因。** 角点抖动 σ 与转移误差近似成正比（σ = 1 px 对应 0.93 px），
这是误差的正常来源。但目标矩形选错时，误差卡在 95 px 左右的平台上：
抖动从 0 增到 5 px，误差只从 95.35 变到 96.07。也就是说，
把角点标定得再准也无法补救宽高比选错。

**处理方法。** 调整优化顺序：先保证目标矩形正确，再追求角点精度。
两者的量级差约两个数量级。

---

## 五、实现细节

### 问题 10：`full_matrices` 改动后会不报错地返回错误结果

**现象与原因。** DLT 矩阵是 `2n×9` 的长方阵，`np.linalg.svd` 只返回
`min(2n, 9)` 个奇异值，真正恒为 0 的第 9 个奇异值不在返回值里。
写成 `full_matrices=False` 时 `V` 被截断，`vh[-1]` 从第 9 行变成第 8 行，
重投影误差从 2e−09 涨到 606.78 px，且流程不会报错。

顺带纠正一处容易记反的事实：`np.linalg.svd` 的默认值本来就是 `True`，
所以删掉这个参数不影响结果，危险的是把它显式改成 `False`。

**处理方法。** 代码里保留显式的 `full_matrices=True` 并加注释说明原因，
让这个约定在代码里可见，不必让读者去查 NumPy 默认值。

### 问题 11：用 `singular_values` 的最小值验证 `A h ≈ 0` 会得出错误判断

**现象与原因。** `singular_values` 只有一个最小值 0.807，看起来离 0 很远，
于是容易怀疑求解有问题。真正的原因是这个数组里根本没有那个恒为 0 的奇异值，
0.807 是 σ₈。

**处理方法。** 改用直接证据：打印 `‖A · vh[-1]‖`，实测约 2.39e−11，
这才是"`A h ≈ 0` 成立"的证据。

---

## 六、数值与工程问题

### 问题 12：DLT 的退化判据依赖坐标量级

**现象与原因。原来的判据是 `np.linalg.matrix_rank` 作用在未归一化的 DLT 矩阵上，
秩低于 8 就报错。这个判据的容差随最大奇异值缩放，因此同一个几何配置、
用不同的坐标单位表达，会得到不同的结论。实测在坐标放大 1e4 倍时秩掉到 7 并报错，
而此时的解确实已经崩坏（n = 12 时重投影误差达到 10² ~ 10³ 像素），
所以它并不是误报；问题在于它把两件不同的事混成了一个错误信息：
几何本身退化（三点共线），和坐标量级压垮了数值精度。

**处理方法。** 拆成两个判据，按顺序检查，报错信息分别说明原因：

1. **几何判据**（尺度无关）。把 DLT 矩阵建在 Hartley 归一化坐标下再判秩，
 秩低于 8 说明至少三点共线或两点重合，重新缩放无法解决。
2. **数值判据**。在求解实际使用的矩阵上判秩，秩低于 8 说明坐标量级使 `A` 病态，
   报错信息提示改用 `normalize=True` 或重新缩放输入。

两者仍以 `ValueError` 抛出，因此"求解失败"的分类不变。
实测改动后消融实验 18 个配置档位的成功率与改动前完全一致
（坐标放大 1e4 倍时基础 DLT 42%、1e5 倍时 0%，归一化版本全程 100%）。

### 问题 13：手写 warp 在多百万像素图上内存峰值过高

**现象与原因。** 逆映射一次性生成整幅目标尺寸的坐标数组，
加上投影结果、逐像素权重和索引列表同时驻留，12 MP 灰度图峰值 2424 MB、
彩色图 3576 MB。收尾处的 `np.clip(np.rint(x), ...)` 还会再各生成一份全尺寸临时数组。

**处理方法。** 目标图像按 64 行分条带处理，每条带只生成自己的坐标数组；
收尾的取整与裁剪改为原地运算（`np.rint(x, out=x)`、`np.clip(..., out=x)`）。
改动后 12 MP 峰值降到 136 MB（灰度）和 358 MB（彩色），
`warp_ms` 中位数从 101 ms 降到 69 ms。

分块只是重新组织计算顺序，不改变任何近似，因此输出必须逐位相同。
实测汇总表 45 行的非计时列零变化，`tests/test_warping.py` 也锁住了这一点
（不同块高必须给出完全相同的数组）。
