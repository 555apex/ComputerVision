## 第一章加分作业

射影矩阵自编程实现:

### 1.图像预处理

选择开放数据集或者自行拍摄一些斜向角度的图片，通过opencv等一些库进行图像预处理，可以适当的降低分辨率来简化运算，得出斜图像的像素矩阵，然后标定四个角点坐标，正视照片的角点固定

### 2.算法原理解析

自编程实现射影矩阵求解函数，通过四个角点的对应关系，求解出摄影矩阵$H$，具体算法思路如下，对应角点的映射关系为${x}' =Hx$ ，设$H$的一般形式如下：
$$
H=
\begin{pmatrix}
 h_{11} &h_{12}  &h_{13} \\
 h_{21}&  h_{22} & h_{23} \\
  h_{31} & h_{32}  & h_{33}
\end{pmatrix}
$$
其中常取 $h_{33}=1$，这是一个**规范化约定**（gauge fixing）：利用齐次坐标的尺度不确定性把整体尺度固定下来，此时射影矩阵只有 8 个自由度需要求解。若不作此约定，待解向量 $\vec h$ 有 9 个分量，属于 9 个未知量、8 个自由度的求解问题；下述理论中不以 $h_{33}=1$ 的特例书写

对一组点$(x_i,y_i)$和对应的$({x}'_{i},{y}'_{i})$对应使用射影变换展开如下：
$$
{x}'_{i}=\frac{h_{11}x_{i}+h_{12}y_{i}+h_{13}}{h_{31}x_{i}+h_{32}y_{i}+h_{33}},\quad {y}'_{i}=\frac{h_{21}x_{i}+h_{22}y_{i}+h_{23}}{h_{31}x_{i}+h_{32}y_{i}+h_{33}}   
$$
交叉相乘消去分母，整理为关于向量$\vec{h}$的线性方程组$\mathbf{A}\vec{h}=0$形式，其中：
$$
\vec{h}=
{\begin{pmatrix}
  h_{11}&h_{12}  &h_{13}  &h_{21}  &h_{22}  &h_{23}  &h_{31}  & h_{32} &h_{33}
\end{pmatrix}}^T
$$

$$
A_i=\begin{pmatrix}
  x_{i}&y_{i}  &  1&  0&  0&  0&  -x_i{x}'_i & -y_i{x}'_i &-{x}'_i \\
  0&  0&  0&  x_i&  y_i&  1&-x_i{y}'_i  &-y_i{y}'_i  &-{y}'_i
\end{pmatrix}
$$

$A_i$即为每一组对应点的参数矩阵，对于一般的问题来说总共有n组这样的对应矩阵，本题目为n=4的情况，所以$\mathbf{A}$是一个8×9阶的矩阵:
$$
A={\begin{bmatrix}
  A_1& A_2 &A_3  &A_4
\end{bmatrix}}^T
$$
求解线性方程组得出h的解空间就得到了射影矩阵$H$

#### 资料参考

上述直接做线性变换DLT求解是可行的，但是下面也有几个求解思路和约束要求：

**SVD 求解**

由于齐次坐标的尺度不确定性，*H* 是齐次等价类，只有 **8 个自由度**（即待解向量 $\vec h$ 有 9 个分量，多出的 1 个尺度自由度由 $\|\vec h\|=1$ 或 $h_{33}=1$ 固定）。对 *A* 进行**奇异值分解**：

$A=UΣV^T$

解 **h** 即为 *V* 的**最后一列**（对应最小奇异值的右奇异向量）。将 **h** 重塑为 3×3 矩阵即得 *H* 。

**归一化 DLT（数值稳定版）**&#8203;

直接 DLT 对坐标尺度敏感，实际应用中需先对坐标做**归一化预处理**：

1. **平移**：将点集质心移至原点
2. **缩放**：缩放点集使其到原点的平均距离为 $\sqrt{2}$（Hartley 标准取值，归一化后坐标 RMS 恰为 1，条件数最优）
3. 对归一化后的点执行标准 DLT
4. 反归一化： $H={T}'^{-1}\tilde{H}T$，其中$T,{T}'$分别为源点和目标点的归一化变换矩阵

**最小点数要求**

- 每对点提供 **2 个独立方程**
- *H* 有 **8 个自由度**
- 因此至少需要 **4 对对应点，且其中任意 3 点不共线**
- 若 *n*>4 ，用 SVD 求最小二乘解，提高鲁棒性

> **本作业实现选择**：采用不固定 $h_{33}$ 的 9 未知量齐次 SVD 求解（对 $A$ 做 SVD 取 $V$ 最后一列），以避免真实 $h_{33}=0$ 时 $h_{33}=1$ 约定失效，并便于直接推广到 $n>4$ 的最小二乘；同时保留 $h_{33}=1$ 的非齐次解作为数值对照。

### 3.输出要求

输入一张斜图片，经过第一步预处理后，输出分辨率，像素矩阵和四个角点坐标，经摄影矩阵运算后输出正图片的像素矩阵